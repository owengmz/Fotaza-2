//- este mixin se encarga de mostrar mensajes de alerta en la pagina */
if locals.flash
  .alert(class=`alert--${locals.flash.tipo || 'info'}`)
    each mensaje in locals.flash.mensajes
      p= mensaje